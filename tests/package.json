{
  "name": "tokenproxy-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for tokenproxy embeddings endpoint",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "devDependencies": {
    "jsdom": "30.0.1",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
